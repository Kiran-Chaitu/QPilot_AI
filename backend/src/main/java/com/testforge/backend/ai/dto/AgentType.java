package com.testforge.backend.ai.dto;

/** The specialized AI "agents" in the multi-agent pipeline (see PROJECT_PLAN.md). */
public enum AgentType {
    CODE_SUMMARY,
    TEST_GENERATION,
    SECURITY_ANALYSIS,
    RISK_SCORE
}
