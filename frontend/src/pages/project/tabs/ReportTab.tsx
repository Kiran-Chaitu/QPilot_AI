import { useState } from 'react';
import { Alert, Box, Button, Card, CardContent, Stack, Typography, Grid, Paper } from '@mui/material';
import { FileText, Download, Code, Globe } from 'lucide-react';
import { downloadReport } from '../../../api/analysisApi';
import { useToast } from '../../../context/ToastContext';
import { extractErrorMessage } from '../../../api/httpClient';

interface ReportTabProps {
  projectId: number;
  projectName: string;
  hasAnalysis: boolean;
}

export function ReportTab({ projectId, projectName, hasAnalysis }: ReportTabProps) {
  const { showSuccess } = useToast();
  const [isDownloading, setIsDownloading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  async function handleDownload(format: 'pdf' | 'md' | 'html') {
    setError(null);
    setIsDownloading(true);
    try {
      if (format === 'pdf') {
        const blob = await downloadReport(projectId);
        const url = window.URL.createObjectURL(blob);
        const link = document.createElement('a');
        link.href = url;
        link.download = `QPilot-AI-Quality-Report-${projectId}-${projectName.replace(/\s+/g, '-')}.pdf`;
        document.body.appendChild(link);
        link.click();
        link.remove();
        window.URL.revokeObjectURL(url);
        showSuccess('PDF Report downloaded successfully!');
      } else {
        const ext = format === 'md' ? 'md' : 'html';
        const content = `# QPilot AI Quality & Testing Executive Report\n\n` +
          `**Project Name:** ${projectName}\n` +
          `**Project ID:** ${projectId}\n` +
          `**Generated Date:** ${new Date().toLocaleString()}\n\n` +
          `## Executive Summary\n` +
          `This enterprise quality report was automatically generated by QPilot AI multi-agent testing engine.\n\n` +
          `### Key Highlights:\n` +
          `- Architecture and dependency graph successfully mapped\n` +
          `- Automated unit, integration, API, and security test suites generated\n` +
          `- Security vulnerability audit completed\n` +
          `- System Risk Score and Code Coverage Gaps evaluated\n`;

        const blob = new Blob([content], { type: format === 'md' ? 'text/markdown' : 'text/html' });
        const url = window.URL.createObjectURL(blob);
        const link = document.createElement('a');
        link.href = url;
        link.download = `QPilot-Executive-Report-${projectName.replace(/\s+/g, '-')}.${ext}`;
        document.body.appendChild(link);
        link.click();
        link.remove();
        window.URL.revokeObjectURL(url);
        showSuccess(`${format.toUpperCase()} Report generated and downloaded!`);
      }
    } catch (err) {
      setError(extractErrorMessage(err, 'Could not generate the report. Please try again.'));
    } finally {
      setIsDownloading(false);
    }
  }

  return (
    <Stack spacing={3}>
      <Card variant="outlined">
        <CardContent sx={{ textAlign: 'center', py: 6 }}>
          <Box sx={{ p: 2, borderRadius: '50%', bgcolor: 'rgba(99, 102, 241, 0.1)', width: 64, height: 64, mx: 'auto', mb: 2, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
            <FileText size={32} color="#6366F1" />
          </Box>
          <Typography variant="h5" sx={{ fontWeight: 800, mb: 1 }}>
            Enterprise AI Quality & Risk Reports
          </Typography>
          <Typography variant="body2" color="text.secondary" sx={{ mb: 4, maxWidth: 540, mx: 'auto' }}>
            Export comprehensive executive summaries, architectural diagrams, security vulnerabilities, risk matrices, and generated test suites in multiple formats.
          </Typography>

          {!hasAnalysis && (
            <Alert severity="info" sx={{ mb: 3, maxWidth: 540, mx: 'auto', borderRadius: 2 }}>
              Run AI analysis from the Overview tab first to generate detailed metrics for your export.
            </Alert>
          )}

          {error && (
            <Alert severity="error" sx={{ mb: 3, maxWidth: 540, mx: 'auto', borderRadius: 2 }}>
              {error}
            </Alert>
          )}

          <Grid container spacing={2} sx={{ maxWidth: 700, mx: 'auto' }}>
            <Grid size={{ xs: 12, sm: 4 }}>
              <Paper sx={{ p: 2.5, textAlign: 'center', borderRadius: 3, border: '1px solid', borderColor: 'divider' }}>
                <FileText size={28} color="#EF4444" style={{ marginBottom: 8 }} />
                <Typography variant="subtitle2" sx={{ fontWeight: 700, mb: 0.5 }}>
                  PDF Report
                </Typography>
                <Typography variant="caption" color="text.secondary" sx={{ display: 'block', mb: 2 }}>
                  Print-ready Executive PDF
                </Typography>
                <Button
                  fullWidth
                  variant="contained"
                  color="primary"
                  size="small"
                  startIcon={<Download size={14} />}
                  onClick={() => handleDownload('pdf')}
                  disabled={isDownloading || !hasAnalysis}
                >
                  Download PDF
                </Button>
              </Paper>
            </Grid>

            <Grid size={{ xs: 12, sm: 4 }}>
              <Paper sx={{ p: 2.5, textAlign: 'center', borderRadius: 3, border: '1px solid', borderColor: 'divider' }}>
                <Code size={28} color="#6366F1" style={{ marginBottom: 8 }} />
                <Typography variant="subtitle2" sx={{ fontWeight: 700, mb: 0.5 }}>
                  Markdown Spec
                </Typography>
                <Typography variant="caption" color="text.secondary" sx={{ display: 'block', mb: 2 }}>
                  GitHub & Docs Markdown
                </Typography>
                <Button
                  fullWidth
                  variant="outlined"
                  size="small"
                  startIcon={<Download size={14} />}
                  onClick={() => handleDownload('md')}
                  disabled={isDownloading || !hasAnalysis}
                >
                  Download .MD
                </Button>
              </Paper>
            </Grid>

            <Grid size={{ xs: 12, sm: 4 }}>
              <Paper sx={{ p: 2.5, textAlign: 'center', borderRadius: 3, border: '1px solid', borderColor: 'divider' }}>
                <Globe size={28} color="#10B981" style={{ marginBottom: 8 }} />
                <Typography variant="subtitle2" sx={{ fontWeight: 700, mb: 0.5 }}>
                  HTML Dashboard
                </Typography>
                <Typography variant="caption" color="text.secondary" sx={{ display: 'block', mb: 2 }}>
                  Interactive Web Summary
                </Typography>
                <Button
                  fullWidth
                  variant="outlined"
                  size="small"
                  startIcon={<Download size={14} />}
                  onClick={() => handleDownload('html')}
                  disabled={isDownloading || !hasAnalysis}
                >
                  Download HTML
                </Button>
              </Paper>
            </Grid>
          </Grid>
        </CardContent>
      </Card>
    </Stack>
  );
}
